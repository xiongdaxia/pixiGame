//  存放全局的常量
const settings = {
    //  接线柱的生效范围
    BINDPOST_WIDTH: 40,
    BINDPOST_HEIGHT: 40,
    // 画虚线的颜色和宽度
    DOTTED_COLOR: 0xff1493,
    DOTTED_WIDTH: 6,
    // 实线的距离间隔
    REALLNE_DISTANCE: 50
};
export default settings;
